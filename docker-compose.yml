services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: avenue10
    ports:
      - "5433:5432"
    volumes:
      - avenue10_data:/var/lib/postgresql/data

volumes:
  avenue10_data:
